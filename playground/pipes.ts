// They don't need to be complicated, just think of them like a function that returns you something new.

function uppercase(string) {
    return string.toUpperCase();
}

const aname: string = uppercase('todd');
console.log(aname); //uppercase e.g. TODD